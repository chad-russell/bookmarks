# TreeView Component Contract

## Props

- `folders`: `Folder[]` (the array of folders to display)
- `bookmarks`: `Bookmark[]` (the array of bookmarks to display)
- `onFolderClick`: `(folder: Folder) => void` (callback for when a folder is clicked)
